"use strict"; // treat all JS code as newer version

// alert("hello") // we are using nodejs, not browser

console.log(3
    + 3) // code is not readable or readable should be high 

    console.log("Saugat") //readable
    
    let name = "Purnima"
    let age = 16
    let isLoggedIn = false
    let state;

    // number => 2 to power 53
    // bigint
    // string => ""
    // boolean => true/false
    // null is standalone value
    // undefined =>  unsigned bhako xaina value
    // symbol => uniqueness define garna


    // object

    console.log(typeof undefined);// undefined
    console.log(typeof null);// object
